# Core web framework
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
google-auth

# RAG / ML stack
sentence-transformers==2.2.2
huggingface_hub==0.24.5
transformers==4.41.2         # compatible with s-t 2.2.x
torch==2.2.2                 # CPU wheel is fine on Render; pin for stability
scikit-learn==1.4.2
numpy==1.26.4
tiktoken==0.7.0              # required by your token-based chunker
pypdf==4.2.0                 # PDF ingestion

# Vector DB (v3 client; import is `from pinecone import Pinecone`)
pinecone-client==3.0.0

# LLM client
anthropic==0.29.0

# Text processing / splitters
nltk==3.8.1
langchain-text-splitters==0.2.2

# Optional but handy
pandas==2.2.2
structlog==24.1.0
redis==5.0.4
pydantic==2.7.4

# ASGI / prod server
gunicorn==21.2.0
gevent==23.9.1                # only if you run -k gevent

# HTTP client (if you need it explicitly)
aiohttp==3.9.5
PyMuPDF==1.23.26